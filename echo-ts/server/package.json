{
  "name": "echo-ts",
  "version": "0.3.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "watch": "rm -rf dist && npx tsc-watch --onSuccess \"qjs ./dist/echo-ts.js\""
  },
  "author": "Webchefs with Cartesi",
  "devDependencies": {
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.4"
  }
}
